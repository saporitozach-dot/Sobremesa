import { AVPlaybackSource } from 'expo-av';

/** Custom landing background — do not replace without asking. */
export const LANDING_VIDEO: AVPlaybackSource = require('../../assets/video/landing-bg.mp4');

/** 1 = normal speed; lower values = slower (cinematic slo-mo). */
export const LANDING_VIDEO_RATE = 0.55;
